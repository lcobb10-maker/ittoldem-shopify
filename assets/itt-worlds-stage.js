(()=>{const init=()=>{const section=document.querySelector('.itt-x__worlds');if(!section)return;const cards=[...section.querySelectorAll('.itt-x__worldgrid a')];if(!cards.length)return;let ticking=false;const draw=()=>{const r=section.getBoundingClientRect();const vh=innerHeight||1;const p=Math.max(0,Math.min(1,(vh-r.top)/(vh+r.height)));cards.forEach((card,i)=>{const dir=i%2===0?1:-1;const x=(p-.5)*(18+i*2)*dir;const y=Math.sin((p+i*.11)*Math.PI)*-18;card.style.setProperty('--worldX',`${x}vw`);card.style.setProperty('--worldY',`${y}px`);card.style.translate=`${x}vw ${y}px`;});const idx=Math.max(0,Math.min(cards.length-1,Math.floor(p*cards.length)));cards.forEach((c,i)=>c.classList.toggle('is-world-active',i===idx));ticking=false};const on=()=>{if(!ticking){requestAnimationFrame(draw);ticking=true}};addEventListener('scroll',on,{passive:true});addEventListener('resize',on,{passive:true});draw()};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();document.addEventListener('shopify:section:load',init)})();